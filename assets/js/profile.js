import { authState, logoutUser } from './auth.js';
authState(user=>{if(!user){location.href='login.html';return;}const name=user.displayName||user.email?.split('@')[0]||'عميل';document.querySelector('#userName').textContent=`مرحبًا ${name} 👋`;document.querySelector('#userEmail').textContent=user.email||'';document.querySelector('#logoutBtn').onclick=async()=>{await logoutUser();location.href='index.html';};});
