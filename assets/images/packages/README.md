# Dubai package photos
Real photos are configured in `assets/js/offers-data.js` and load from Marriott's public gallery URLs. Upload licensed copies through Admin > Offers > Edit > Upload images to move them into Firebase Storage.
