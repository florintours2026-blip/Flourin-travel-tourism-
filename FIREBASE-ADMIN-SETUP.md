[FIREBASE-ADMIN-SETUP.md](https://github.com/user-attachments/files/30961554/FIREBASE-ADMIN-SETUP.md)
# FLORIN — إعداد لوحة الإدارة

## 1) نشر الموقع
ارفع محتويات هذا المجلد إلى Repository الخاص بك، مع الحفاظ على مجلد `assets`.

## 2) Firebase Admin — خطوة واحدة فقط
لأسباب أمنية، لوحة الإدارة لا تعتمد على البريد الإلكتروني داخل JavaScript.
بعد إنشاء حساب المدير من `register.html`:
1. افتح Firebase Console → Firestore Database.
2. أنشئ collection باسم `admins`.
3. أنشئ document ID يساوي UID لحساب المدير.
4. أضف field:
   - `active` = `true` (boolean)
5. افتح `admin.html` بعد تسجيل الدخول.

بعد هذه الخطوة يمكنك إضافة وتعديل وإخفاء وحذف العروض من لوحة الإدارة بدون تعديل أي كود.

## 3) Firestore Rules
الملف `firestore.rules` يحتوي قواعد الوصول المقترحة:
- العروض: القراءة عامة، الكتابة للمدير فقط.
- الحجوزات: العميل يستطيع إنشاء طلب، والمدير يستطيع قراءته وتحديثه.
- العملاء: المستخدم يقرأ بياناته، والمدير يقرأ الجميع.
- سجل الدخول والخروج: المستخدم يسجل نشاطه، والمدير يقرأ السجل.
يجب نشر هذه القواعد في Firebase Console → Firestore → Rules.

## 4) إدارة العروض
من لوحة الإدارة:
- `استيراد العروض الحالية` ينسخ العروض الموجودة في الموقع إلى Firestore مرة واحدة.
- `+ إضافة عرض` لإنشاء عرض.
- `تعديل` لتعديل السعر والوصف والصورة والخدمات وغير ذلك.
- `حذف` لحذف العرض.
- يمكن إخفاء العرض دون حذفه.

## 5) الصور
حقل الصورة يقبل مسارًا داخل الموقع مثل:
`assets/images/offers/dubai-offer.png`
أو رابط صورة مباشر.
رفع الصور إلى Firebase Storage مفعّل في هذه النسخة:
- من لوحة الإدارة يمكنك اختيار عدة صور من الهاتف.
- الصور تُرفع إلى `offers/{offerId}/`.
- الروابط تُحفظ داخل العرض في `images`.
- يمكن أيضًا إدخال روابط/مسارات يدوية.
- حد الصورة الواحدة 8MB، والصور فقط مسموحة.

بعد إضافة قواعد Storage من ملف `storage.rules` إلى Firebase Console → Storage → Rules، يصبح رفع الصور من الهاتف متاحًا للمدير فقط.
